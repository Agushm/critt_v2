part of 'widgets.dart';

Widget buildGarisPemisah(BuildContext context, {double? height}) {
  return Container(
    width: deviceWidth(context),
    height: height,
    decoration: BoxDecoration(color: bgGrey),
  );
}
